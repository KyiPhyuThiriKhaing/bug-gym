## Question 1
Complete the method `addTwo(int a, int b)` so it returns the sum of two integers.

Read `a b` and print the method result.

### For example:
| **Input** | **Result** |
|:--------- |:-----------|
|3 5|8|
|-4 9|5|

### Starter Code
```java
import java.util.*;

public class Main {
    static int addTwo(int a, int b) {
        // TODO
        return 0;
    }

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        int b = sc.nextInt();
        System.out.println(addTwo(a, b));
    }
}
```
