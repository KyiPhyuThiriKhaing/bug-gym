## Question 2
Write a program that defines a method `isEven(int n)` and prints `Even` if the number is even, otherwise prints `Odd`.

The input is one integer. The output is `Even` or `Odd`.

### For example:
| **Input** | **Result** |
|:--------- |:-----------|
|8|Even|
|13|Odd|
|0|Even|
